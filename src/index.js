const name = "きよ";
const age = 29;

const message1 = `私の名前は${name}です。年齢は${age}です。`;
console.log(message1);

/**
 * アロー関数
 */

const func2 = (str) => {
  return str;
};

console.log(func2("func2です"));
